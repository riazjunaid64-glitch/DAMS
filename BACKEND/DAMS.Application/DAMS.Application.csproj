<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\DAMS.Domain\DAMS.Domain.csproj" />
    <ProjectReference Include="..\DAMS.Infrastructure\DAMS.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="BCrypt.Net-Next" Version="4.0.3" />
    <PackageReference Include="MailKit" Version="4.17.0" />
    <PackageReference Include="SkiaSharp" Version="4.148.0" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- The web-push wire format is internal to the sender but has to be verifiable against
         the standards; the tests decrypt what it produces. -->
    <InternalsVisibleTo Include="DAMS.Application.Tests" />
  </ItemGroup>

</Project>
